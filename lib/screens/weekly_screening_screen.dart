import 'package:flutter/material.dart';

class WeeklyScreeningScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Weekly Screening')),
      body: Center(
        child: Text('Weekly Screening Questions'),
      ),
    );
  }
}