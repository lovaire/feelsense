import 'package:flutter/material.dart';

class DailyScreeningScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Daily Screening')),
      body: Center(
        child: Text('Daily Screening Questions'),
      ),
    );
  }
}